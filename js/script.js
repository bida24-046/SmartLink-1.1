
const cursor=document.querySelector('.cursor-glow');document.addEventListener('mousemove',e=>{if(cursor){cursor.style.left=e.clientX-140+'px';cursor.style.top=e.clientY-140+'px'}});
const nav=document.querySelector('.main-nav'), toggle=document.querySelector('.nav-toggle');if(toggle){toggle.onclick=()=>nav.classList.toggle('open')}
document.getElementById('year')&&(document.getElementById('year').textContent=new Date().getFullYear());
const obs=new IntersectionObserver(entries=>{entries.forEach(en=>{if(en.isIntersecting){en.target.classList.add('visible');const nums=en.target.querySelectorAll('[data-count]');nums.forEach(n=>{let t=+n.dataset.count,c=0,step=Math.ceil(t/48);let id=setInterval(()=>{c+=step;if(c>=t){c=t;clearInterval(id)}n.textContent=c+(t===100?'%':'')},24)})}})},{threshold:.16});document.querySelectorAll('.reveal,.stats-grid').forEach(el=>obs.observe(el));
const canvas=document.getElementById('particleCanvas'),ctx=canvas.getContext('2d');let w,h,particles=[];function resize(){w=canvas.width=innerWidth;h=canvas.height=innerHeight;particles=Array.from({length:Math.min(90,Math.floor(w/18))},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.7,vy:(Math.random()-.5)*.7,r:Math.random()*2+1}))}resize();addEventListener('resize',resize);function animate(){ctx.clearRect(0,0,w,h);particles.forEach((p,i)=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>w)p.vx*=-1;if(p.y<0||p.y>h)p.vy*=-1;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle=i%2?'rgba(17,184,232,.55)':'rgba(255,24,24,.48)';ctx.fill();for(let j=i+1;j<particles.length;j++){let q=particles[j],dx=p.x-q.x,dy=p.y-q.y,d=Math.hypot(dx,dy);if(d<120){ctx.strokeStyle='rgba(255,255,255,'+(1-d/120)*.12+')';ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(q.x,q.y);ctx.stroke()}}});requestAnimationFrame(animate)}animate();
